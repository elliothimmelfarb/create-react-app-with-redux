// import * as types from '../Actions/point.actions';

const initialState = {
  coords: [
    [51.505, -0.09],
  ],
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
